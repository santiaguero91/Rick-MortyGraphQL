import styled from "@emotion/styled";
import { motion } from "framer-motion";

export const LoadingMainDiv = styled(motion.div)`
  color: transparent; 
  background: linear-gradient(to right, #3D7068 1%, #14453D); 
  -webkit-background-clip: text;
  background-clip: text;
`;

